## This is project for third task (Alien's numbers encoding).

I have not provided maven project with run plugin, so you need to run `com.kisel.algo.App.java`.

Also project is provided with jUnit test. 
